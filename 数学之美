一、两数之和
建立值——索引值键值对，找到值后，通过字典找到索引值
return [map[target-nums[i]], i]
map[nums[i]] = i

二、三数之和
先排序
如果nums[i]==nums[i-1]可以跳过
然后通过i、l、r三个指针判断nums[i]+nums[l]+nums[r]与0的关系：
最后添加数组res[]中
记得需要分别保证i>0或者l<r，i+1或者l+1或者r-1
